@using Avicola.Office.Entities
@using Avicola.Office.Services.Interfaces
@using Framework.Common.Web.Extensions
@model Guid?
@{
    var service = IocContainer.GetContainer().Get<IStageService>();
    var selectList = service.GetAll().ToSelectListItem(Model, x => x.Id, x => x.Name, x => x.Id.ToString());
}


@Html.DropDownList("", selectList, "Seleccione Etapa", new { @class = "form-control select2-control" })
