@using Avicola.Office.Entities
@using Framework.Common.Web.Extensions
@model Guid?
@{
    var list = new List<KeyValuePair<Guid, string>>
    {
        new KeyValuePair<Guid, string>(Stage.BREEDING, "Cría y Pre-cría") ,
        new KeyValuePair<Guid, string>(Stage.POSTURE, "Postura") 
    };
}

@Html.DropDownList("", new SelectList(list, "Key", "Value", Model), "Seleccione Etapa", new { @class = "form-control select2-control" })
