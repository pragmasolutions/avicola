@using Avicola.Office.Entities
@using Avicola.Office.Services.Interfaces
@using Framework.Common.Web.Extensions
@model Guid?

@Html.TextBox("", Model == Stage.BREEDING ? "Cría" : (Model == Stage.REBREEDING ? "Recría" : "Postura"), new { @class = "form-control", @readonly = true })
