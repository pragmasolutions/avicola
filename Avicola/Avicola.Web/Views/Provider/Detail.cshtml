@using Framework.Common.Web.Helpers
@model Avicola.Web.Models.ProviderForm
@{
    ViewBag.Title = "Detalle de Proveedor";
}

<h3>Detalle de Proveedor</h3>
@Html.BackButton("/Provider")
<hr />
<div class="form-horizontal">
    @Html.DisplayForModel()
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            @using (Html.BeginForm("Delete", "Provider", new { id = Model.Id }, FormMethod.Post, new { id = "delete-form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ActionLink("Editar", "Edit", "Provider", routeValues: new { Model.Id }, htmlAttributes: new { @class = "btn btn-primary" })
                <button type="submit" class="btn btn-warning">Eliminar</button>
            }
        </div>
    </div>
</div>


@section scripts
{
    <script>
        $(function () {
            $("#delete-form").on("submit", function () {
                return confirm("Está seguro que desea eliminar este proveedor?  Esta operación no se puede deshacer.");
            });
        });
    </script>
}
