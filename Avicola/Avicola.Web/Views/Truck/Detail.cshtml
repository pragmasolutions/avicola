@using Framework.Common.Web.Helpers
@model Avicola.Web.Models.TruckForm
@{
    ViewBag.Title = "Detalle de Conductor";
}

<h3>Detalle de Camion</h3>
@Html.BackButton("/Truck")
<hr />
<div class="form-horizontal">
    @Html.DisplayForModel()
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            @using (Html.BeginForm("Delete", "Truck", new { id = Model.Id }, FormMethod.Post, new { id = "delete-form" }))
            {
                @Html.AntiForgeryToken()
                @Html.ActionLink("Editar", "Edit", "Truck", routeValues: new { Model.Id }, htmlAttributes: new { @class = "btn btn-primary" })
                <button type="submit" class="btn btn-warning">Eliminar</button>
            }
        </div>
    </div>
</div>


@section scripts
{
    <script>
        $(function () {
            $("#delete-form").on("submit", function () {
                return confirm("Está seguro que desea eliminar este camion?  Esta operación no se puede deshacer.");
            });
        });
    </script>
}
